{
  "name": "plati-landing",
  "private": true,
  "description": "Landing page statique de Plati, publiee sur GitHub Pages",
  "scripts": {
    "build": "mkdir -p dist && tailwindcss -i src/input.css -o dist/styles.css --minify && cp index.html dist/",
    "dev": "tailwindcss -i src/input.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
